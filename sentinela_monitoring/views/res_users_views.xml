<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form_inherit_sip" model="ir.ui.view">
        <field name="name">res.users.form.inherit.sip</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='preferences']" position="inside">
                <group string="Telefonía IP (Grandstream)">
                    <field name="sip_extension" placeholder="Ej. 101"/>
                </group>
                <group string="GPS / Patrulla (SentiCar)">
                    <field name="is_patrol"/>
                    <field name="default_patrol_unit_id" invisible="not is_patrol"
                        options="{'no_open': True}"
                        placeholder="Su celular/unidad habitual..."/>
                    <field name="traccar_device_id" invisible="not is_patrol"
                        string="ID SentiCar (respaldo)"
                        placeholder="Opcional — solo si no usa unidad del catálogo"/>
                    <field name="last_gps_update" readonly="1" invisible="not is_patrol"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
