# Base del Gateway que consume la SPA (se inyecta en build; NEXT_PUBLIC_* es público).
NEXT_PUBLIC_API_BASE=https://api.sentinela.mx

# Pagos — Stripe.js (Sprint 2 / Cobranza · MODO TEST). Clave PUBLICA de prueba (pk_test_...).
# Se usa en el front para tokenizar la tarjeta (historia S2-014). Placeholder; valor real en build.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
