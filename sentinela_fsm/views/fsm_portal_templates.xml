<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Stable FSM Portal Templates (No XPaths to avoid version conflicts) -->

    <!-- List View -->
    <template id="portal_my_services" name="Mis Solicitudes">
        <t t-call="portal.portal_layout">
            <div class="container mt-4">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h3>Mis Solicitudes de Soporte</h3>
                    <a href="/my/services/new" class="btn btn-primary">Levantar Nuevo Ticket</a>
                </div>
                <t t-if="not fsm_orders">
                    <div class="alert alert-info shadow-sm">No tienes tickets registrados.</div>
                </t>
                <div t-if="fsm_orders" class="table-responsive bg-white shadow-sm rounded">
                    <table class="table table-hover mb-0">
                        <thead class="bg-light">
                            <tr>
                                <th>Folio</th>
                                <th>Fecha</th>
                                <th>Problema</th>
                                <th>Estado</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr t-foreach="fsm_orders" t-as="ticket">
                                <td><t t-esc="ticket.name"/></td>
                                <td><span t-field="ticket.create_date" t-options='{"widget": "date"}'/></td>
                                <td><t t-out="ticket.description"/></td>
                                <td>
                                    <span t-if="ticket.stage == 'new'" class="badge rounded-pill bg-info text-dark">Recibido</span>
                                    <span t-if="ticket.stage == 'assigned'" class="badge rounded-pill bg-primary">Asignado</span>
                                    <span t-if="ticket.stage == 'done'" class="badge rounded-pill bg-success">Resuelto</span>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </t>
    </template>

    <!-- Form View -->
    <template id="portal_create_service" name="Nuevo Ticket">
        <t t-call="portal.portal_layout">
            <div class="col-md-8 offset-md-2 mt-4">
                <div class="card shadow border-0">
                    <div class="card-header bg-primary text-white py-3">
                        <h5 class="mb-0"><i class="fa fa-ticket me-2"/> Reportar una Falla Técnica</h5>
                    </div>
                    <div class="card-body p-4">
                        <form action="/my/services/submit" method="post">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            
                            <div class="mb-3" t-if="subscriptions">
                                <label class="form-label fw-bold">Contrato Afectado (Opcional):</label>
                                <select name="subscription_id" class="form-select">
                                    <option value="">-- Seleccionar Servicio --</option>
                                    <t t-foreach="subscriptions" t-as="sub">
                                        <option t-att-value="sub.id"><t t-esc="sub.name"/> - <t t-esc="sub.product_id.name"/></option>
                                    </t>
                                </select>
                            </div>

                            <div class="mb-4">
                                <label class="form-label fw-bold">Describa el problema:</label>
                                <textarea class="form-control" name="description" rows="5" required="required" placeholder="Ej: No tengo internet..."></textarea>
                            </div>
                            <div class="mb-4 form-check bg-light p-3 rounded border">
                                <input type="checkbox" class="form-check-input ms-0" name="urgent" id="urgent"/>
                                <label class="form-check-label text-danger fw-bold ms-2" for="urgent">¡URGENTE! Sitio sin servicio</label>
                            </div>
                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary btn-lg">Enviar Reporte</button>
                                <a href="/my/services" class="btn btn-link mt-2 text-muted">Cancelar y volver</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <!-- Página pública de reporte (sin sesión): el cliente inicia por WhatsApp.
         Layout autocontenido con web.assets_frontend (Bootstrap+FA) para NO depender de 'website'. -->
    <template id="portal_report_public" name="Reportar una falla">
        &lt;!DOCTYPE html&gt;
        <html lang="es">
            <head>
                <meta charset="utf-8"/>
                <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"/>
                <link rel="icon" type="image/png" href="/sentinela_fsm/static/description/icon_tech_192.png"/>
                <title>Reportar una falla — Sentinela</title>
                <t t-call-assets="web.assets_frontend" t-js="false"/>
                <style>
                    body { background: #f0f2f5; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica; }
                    .report-header { background: #1a237e; color: white; padding: 16px; text-align: center; }
                    .report-card { background: white; border-radius: 12px; box-shadow: 0 2px 8px rgba(0,0,0,0.06); }
                </style>
            </head>
            <body>
                <div class="report-header shadow-sm">
                    <span><i class="fa fa-shield me-2"/> <b>SENTINELA</b></span>
                </div>
                <div class="container my-4" style="max-width: 560px;">
                    <div class="text-center mb-4">
                        <h3 class="fw-bold">Reportar una falla</h3>
                        <p class="text-muted">Cuéntanos qué está pasando con tu servicio y levantamos tu reporte.</p>
                    </div>

                    <!-- Opción 1: WhatsApp (rápido, sin cuenta) -->
                    <div class="report-card mb-4">
                        <div class="p-4 text-center">
                            <i class="fa fa-whatsapp text-success" style="font-size: 3rem;"/>
                            <h5 class="fw-bold mt-2">Reportar por WhatsApp</h5>
                            <p class="text-muted">La forma más rápida. Un asesor te responde en horario de oficina.</p>
                            <a t-att-href="wa_url" target="_blank" rel="noopener" class="btn btn-success btn-lg">
                                <i class="fa fa-whatsapp me-2"/> Abrir WhatsApp
                            </a>
                        </div>
                    </div>

                    <!-- Opción 2: Portal (clientes con cuenta) -->
                    <div class="report-card">
                        <div class="p-4 text-center">
                            <i class="fa fa-user-circle text-primary" style="font-size: 3rem;"/>
                            <h5 class="fw-bold mt-2">¿Ya tienes cuenta del portal?</h5>
                            <p class="text-muted">Inicia sesión para levantar tu ticket y darle seguimiento.</p>
                            <a href="/my/services/new" class="btn btn-outline-primary btn-lg">
                                Entrar a mi portal
                            </a>
                        </div>
                    </div>
                </div>
            </body>
        </html>
    </template>
</odoo>