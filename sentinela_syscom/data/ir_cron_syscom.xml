<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
        <!-- Único cron activo y funcional: actualización diaria de precios y stock. -->
        <record id="ir_cron_syscom_sync" model="ir.cron">
            <field name="name">Syscom: Update Prices and Stock</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="state">code</field>
            <field name="code">model._cron_update_syscom_products()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 02:00:00')"/>
        </record>
        <!-- Eliminado v18.0.1.2.0: cron `Syscom: Sync Order Logistics` apuntaba a
             método inexistente `_cron_sync_syscom_logistics`. Si se necesita
             en el futuro, implementar primero el método. -->
</odoo>
